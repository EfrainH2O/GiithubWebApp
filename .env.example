# Puerto del servidor (entero entre 1 y 65535)
PORT=3000
# Entorno de ejecución: development | staging | production | test
NODE_ENV=development
# Secreto para firmar JWTs (no puede estar vacío)
JWT_SECRET=changeme
# Bandera de feature: true/false, 1/0, yes/no, on/off
FEATURE_X_ENABLED=false
